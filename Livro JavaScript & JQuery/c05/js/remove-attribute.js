var firstItem = document.getElementById('one'); // Obtém o 1º item
if (firstItem.hasAttribute('class')) {   // Se tiver um atributo class
    firstItem.removeAttribute('class')  // Remove seu atributo class
}